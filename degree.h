#ifndef DEGREE_H
#define DEGREE_H

#include <iostream>

/* Required header file for degree containing the enum data type.
   Make sure to reference where needed. */

using namespace std;

enum DegreeProgram { SECURITY, NETWORK, SOFTWARE };

#endif
